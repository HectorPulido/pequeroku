export const FSWS = {
	waitOpenIntervalMs: 100,
	openTimeoutMs: 20000,
	callTimeoutMs: 20000,
};

export const METRICS = {
	pollMs: 1000,
	maxPoints: 300,
};

export const ACTION_DELAYS = {
	cloneRepoWaitMs: 5000,
};
